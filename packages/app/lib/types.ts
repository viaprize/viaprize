export interface PactDetail {
  address: string;
  balance: number;
  blockHash: string;
  name: string;
  resolvable: boolean;
  resolved: boolean;
  safe: string;
  terms: string;
  transactionHash: string;
}
