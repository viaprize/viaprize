import { AppShell } from '@mantine/core';
import React from 'react';

function onboarding() {
    return (
        <AppShell>
            <h1>Onboarding</h1>
        </AppShell>
    );
}

export default onboarding;